# Support

Use GitHub issues for reproducibility questions, suspected data inconsistencies, documentation corrections, and runner bugs. Include the operating system, Python version, Playwright version, Chromium version, exact command, and generated `run_environment.json` and `run_manifest.json` files.

Questions about the editorial interpretation belong with the companion Metricfixer publication rather than the code repository.
